# mapreduce-single-source-shortest-path

Given a graph and a node “s”, find the shortest distances together with the paths from “s” to all nodes. 

The implementation takes idea from Dijkstra algorithm.

